# Marstek Cloud Home Assistant Integration - Development Requirements
# Install with: pip install -r requirements-dev.txt

# Include production requirements
-r requirements.txt

# Note: Home Assistant dependencies are not included here due to installation issues
# The integration tests use mocking to avoid requiring the full Home Assistant installation
# If you need Home Assistant for testing, install it separately:
# pip install homeassistant --no-deps

# Development and testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# Code quality tools
black>=22.0.0
flake8>=5.0.0
isort>=5.10.0
mypy>=1.0.0

# Documentation
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0

# Pre-commit hooks
pre-commit>=2.20.0

# Environment management
python-dotenv>=1.0.0

# Additional testing utilities
aioresponses>=0.7.0
freezegun>=1.2.0
